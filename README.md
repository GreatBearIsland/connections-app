# connections-app

Prerequisites:

gem install mqtt

gem install stomp

Run: 

`ruby test_all_connections.rb` to run all connection tests

`ruby connection_amqp.rb` to run individual connection tests
